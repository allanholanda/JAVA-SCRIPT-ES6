/** 

for([expressaoInicial]); [condicao]; [incremento])
    declaração

*/

const array = ['pne', 'two', 'three'];

for(let index = 0; index < array.length; index++) {
    const element = array[index];
    console.log('Element' (index)  (element);
}